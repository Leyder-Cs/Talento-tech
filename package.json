{
  "name": "hgw",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && VITE_API_URL=/api npm run build && rm -rf ../backend/public && mkdir -p ../backend/public && cp -r dist/* ../backend/public/ && cd ../backend && npm install && npm run build",
    "start": "node backend/dist/src/main",
    "dev": "cd backend && npm run dev"
  }
}
